# Gemini API 키 — 서버 사이드 전용 (절대 git 커밋 X)
# 로컬 dev: .env.local 에 동일 키 이름으로 저장
# Vercel 배포: 프로젝트 환경변수에 등록 (모든 환경 - Production/Preview/Development)
GEMINI_API_KEY=

# 선택: 클라이언트 직접 호출 fallback 시 (보안상 비권장 — MVP에선 사용 X)
# VITE_GEMINI_API_KEY=
